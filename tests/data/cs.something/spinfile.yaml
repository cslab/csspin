# This is the spinfile.yaml of spin.
minimum-spin: "0.2"

plugins:
  - spin.builtin:
      - eggs
      - pytest
      - ce15

eggs:
  requirements: ["cs.web"]
  extra_index_url: "https://packages.contact.de/apps/15.4/+simple"
